apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tech-db
  namespace: apps
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 128Mi

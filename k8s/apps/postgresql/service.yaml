apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: apps
spec:
  selector:
    app: postgresql
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None  # headless service for StatefulSet DNS

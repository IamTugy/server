apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: recipes-mcp
  namespace: apps
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`mcp.tugy.dev`)
      kind: Rule
      services:
        - name: recipes-mcp
          port: 80
